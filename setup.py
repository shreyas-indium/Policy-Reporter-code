from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Extracting data from table in pdf files into specific formats ',
    author='APS',
    license='',
)
